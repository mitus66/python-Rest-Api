# autors/urls.py
from django.urls import path
from . import views

app_name = 'autors'

urlpatterns = [
    # Ви можете додати ваші URL-адреси тут, наприклад:
    # path('', views.index, name='index'),
]
Як запускати:

1. Збірка образу:
docker-compose build

2. Запуск CLI в інтерактивному режимі:
docker-compose run addressbook-cli
(після чого буде запущений addressbook.py, і можна вводити команди)

3. Зупинка або видалення контейнера:
docker-compose down